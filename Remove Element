    public int removeElement(int[] nums, int val) {
        int k = 0; // index for next valid placement

        for (int i = 0; i < nums.length; i++) {
            if (nums[i] != val) {
                nums[k] = nums[i]; // overwrite
                k++;
            }
             }

        return k; // number of valid elements
    }
}




use k as length to return and overewriye new array with k value.
